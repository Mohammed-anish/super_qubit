name: super_qubit
description: Hierarchical state management for Flutter. Manage multiple states with event propagation control, cross-communication, and parent-child event handling patterns.
version: 1.0.0
homepage: https://github.com/Mohammed-anish/super_qubit
repository: https://github.com/Mohammed-anish/super_qubit
issue_tracker: https://github.com/Mohammed-anish/super_qubit/issues

topics:
  - state-management
  - bloc
  - event-driven
  - hierarchical
  - cubit

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.15.0
  nested: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  test: ^1.25.6
  mocktail: ^1.0.4
  devtools_extensions: ^0.4.0
  devtools_app_shared: ^0.4.0

